import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VisualizarvehiculoPage } from './visualizarvehiculo.page';

describe('VisualizarvehiculoPage', () => {
  let component: VisualizarvehiculoPage;
  let fixture: ComponentFixture<VisualizarvehiculoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizarvehiculoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  /*it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
